# .github/workflows/build.yml
name: Build Windows EXE

on:
  push:
    tags: [ "v*" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: Add MSVC to PATH
      uses: ilammy/msvc-dev-cmd@v1

    - name: Build Release x64
      shell: cmd
      run: |
        cl /EHsc /O2 /DUNICODE /D_UNICODE ^
           /I"%GITHUB_WORKSPACE%\res" ^
           "%GITHUB_WORKSPACE%\src\*.cpp" ^
           "%GITHUB_WORKSPACE%\res\resource.rc" ^
           /link /OUT:DesktopIconController.exe

    - name: Upload artifact (manual run)
      uses: actions/upload-artifact@v4
      if: github.event_name == 'workflow_dispatch'
      with:
        name: DesktopIconController
        path: DesktopIconController.exe

    - name: Upload to release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: DesktopIconController.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}# .github/workflows/build.yml
name: Build Windows EXE

on:
  push:
    tags: [ "v*" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: Add MSVC to PATH
      uses: ilammy/msvc-dev-cmd@v1

    - name: Build Release x64
      shell: cmd
      run: |
        cl /EHsc /O2 /DUNICODE /D_UNICODE ^
           /I"%GITHUB_WORKSPACE%\res" ^
           "%GITHUB_WORKSPACE%\src\*.cpp" ^
           "%GITHUB_WORKSPACE%\res\resource.rc" ^
           /link /OUT:DesktopIconController.exe

    - name: Upload artifact (manual run)
      uses: actions/upload-artifact@v4
      if: github.event_name == 'workflow_dispatch'
      with:
        name: DesktopIconController
        path: DesktopIconController.exe

    - name: Upload to release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: DesktopIconController.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
